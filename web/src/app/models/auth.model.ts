/**
 * Miroir TypeScript des schemas de `interface/api/routers/auth.py`.
 * Pas de codegen : à mettre à jour en même temps que le schéma Pydantic.
 */

export interface SessionInfo {
  actor_id: string;
}
